import Layout from "../MainLayout"
import { SensorDashboard } from "../SensorDashboard"

export const SensorPage = () => {

    return  (
        <Layout>
            <SensorDashboard/>
        </Layout>
    )
}