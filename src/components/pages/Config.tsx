import { ConfigurationModule } from "../Config/Config"
import Layout from "../MainLayout"
export const Config = () => {

    return  (
        <Layout>
            <ConfigurationModule/>
        </Layout>
    )
}