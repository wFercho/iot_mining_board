import Layout from "../MainLayout"
import { MineDashboard } from "../Mines/MineDashBoards"

export const MinesPage = () => {

    return  (
        <Layout>
            <MineDashboard/>
        </Layout>
    )
}